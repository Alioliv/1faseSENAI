/*3. Conte quantos números pares existem entre 1 e 50.*/
function contador(){
    let num = 1
    let numpares = 0

    while(num <= 50){
        if (num % 2 === 0) {
            numpares += numero 
          }
    }
}